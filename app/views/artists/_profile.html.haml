.artist__profile
  = image_tag(@artist.image_url(:big), alt: @artist)
  .artist__profile__title= @artist.title
%div
  = render partial: 'elements/social_links', object: @artist.social_links
  - if user_admin?
    = link_to 'Edit', edit_artist_path(@artist, social: true), class: 'button'