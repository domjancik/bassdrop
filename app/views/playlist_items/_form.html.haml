= simple_form_for(@playlist_item) do |f|
  = f.error_notification

  .form-inputs
    = f.association :playlist
    = f.association :medium
    = f.input :list_order

  .form-actions
    = f.button :submit
